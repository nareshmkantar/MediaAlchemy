[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: multi-module context / workflow handoff tests
    regression: CP-07 and context engineering regression harness (Phase 10)
